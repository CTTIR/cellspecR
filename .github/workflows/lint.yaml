on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: lint

permissions: read-all

jobs:
  lint:
    uses: CTTIR/.github/.github/workflows/lint.yaml@main
    with:
      # cellspecR started at zero house-linter findings, so the job blocks.
      blocking: true
