% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support.R
\name{cs_feature_support}
\alias{cs_feature_support}
\title{Summarise the signal support of features per image}
\usage{
cs_feature_support(
  x,
  features = cs_features(x, kind = "intensity", statistic = "mean"),
  zero_tol = 0
)
}
\arguments{
\item{x}{A \code{cellspec} object.}

\item{features}{Character vector of feature identifiers. Defaults to all
mean intensity features.}

\item{zero_tol}{Values with absolute value at most \code{zero_tol} count as zero
for \code{fraction_zero}.}
}
\value{
A data frame with one row per image and feature and the columns
\code{image_id}, \code{feature_id}, \code{marker}, \code{compartment}, \code{statistic}
(character), \code{n}, \code{n_na} (integer), \code{fraction_zero}, \code{sd}, \code{mad}, \code{q01},
\code{q50}, \code{q99} (double, computed on non-missing values; \code{NA} when none)
and \code{support} (character, see Details).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Describes, for every image and feature, how much usable variation a
feature carries. A marker whose channel was blank, saturated or not
acquired shows up as \code{"constant"} or \code{"near_constant"} here before any
normalisation or gating hides the problem. Other packages (for example a
gating package) use the \code{support} column to declare markers non-callable.

The \code{support} classes are assigned in this order:
\enumerate{
\item \code{"mostly_na"}: more than half of the values are missing.
\item \code{"constant"}: all non-missing values are identical (for example an
all-zero channel).
\item \code{"near_constant"}: the 1st and 99th percentiles are identical, so at
most about 1\% of cells differ from the rest.
\item \code{"ok"}: otherwise.
}
}
\examples{
x <- cs_example()
cs_feature_support(x)

# A blank channel is reported as constant
x$measurements[, "cell:FOXP3:mean"] <- 0
s <- cs_feature_support(x)
s[s$marker == "FOXP3", c("image_id", "feature_id", "support")]
}
\seealso{
Other validation:
\code{\link[=cs_validate]{cs_validate()}}
}
\concept{validation}
