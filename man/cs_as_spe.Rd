% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop.R
\name{cs_as_spe}
\alias{cs_as_spe}
\title{Convert a cellspec object to SpatialExperiment}
\usage{
cs_as_spe(x, policy = NULL, assay_name = "intensity")
}
\arguments{
\item{x}{A \code{cellspec} object.}

\item{policy}{A signal policy. It is required when a marker has more than
one compartment or statistic; otherwise it can be omitted.}

\item{assay_name}{Name of the main signal assay.}
}
\value{
A \code{SpatialExperiment} with cells as columns, markers as rows,
\code{spatialCoords} containing \code{x} and \code{y}, and a \code{measurements} altExp.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Converts selected signals to the main assay, stores every raw measurement
in an \code{altExp}, and records the cellspec metadata needed for a lossless
round trip.
}
\examples{
if (requireNamespace("SpatialExperiment", quietly = TRUE)) {
  x <- cs_example()
  policy <- cs_signal_policy(cs_markers(x))
  spe <- cs_as_spe(x, policy)
  spe
}
}
\seealso{
Other interoperability:
\code{\link[=cs_as_anndata]{cs_as_anndata()}},
\code{\link[=cs_from_spe]{cs_from_spe()}}
}
\concept{interoperability}
