% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/object.R
\name{cs_features}
\alias{cs_features}
\alias{cs_markers}
\title{Select features and markers by their dictionary entries}
\usage{
cs_features(
  x,
  kind = NULL,
  marker = NULL,
  compartment = NULL,
  statistic = NULL
)

cs_markers(x)
}
\arguments{
\item{x}{A \code{cellspec} object.}

\item{kind, marker, compartment, statistic}{Character vectors of allowed
values, or \code{NULL} for no filter on that column.}
}
\value{
\code{cs_features()}: character vector of \code{feature_id} values in
dictionary order (length 0 when nothing matches). \code{cs_markers()}:
character vector of marker names in order of first appearance.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{cs_features()} returns the identifiers of measurement columns whose
dictionary entries match every supplied filter. Select features through
this function rather than by splitting \code{feature_id} strings; the identifier
layout is not part of the contract downstream code may parse.

\code{cs_markers()} returns the markers that have at least one intensity
feature.
}
\examples{
x <- cs_example()
cs_features(x, kind = "intensity", statistic = "mean")
cs_features(x, marker = "FOXP3")
cs_markers(x)
}
\seealso{
Other object:
\code{\link{cs_accessors}},
\code{\link[=cs_bind]{cs_bind()}},
\code{\link[=cs_example]{cs_example()}},
\code{\link[=cs_merge_tiles]{cs_merge_tiles()}},
\code{\link[=cs_new]{cs_new()}},
\code{\link[=is_cellspec]{is_cellspec()}}
}
\concept{object}
