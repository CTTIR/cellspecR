% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{cs_signal_matrix}
\alias{cs_signal_matrix}
\title{Select one signal per cell and marker}
\usage{
cs_signal_matrix(x, policy, image_id = NULL)
}
\arguments{
\item{x}{A \code{cellspec} object.}

\item{policy}{A \code{cs_signal_policy} created by \code{\link[=cs_signal_policy]{cs_signal_policy()}}.}

\item{image_id}{Optional image identifier. When supplied, only cells from
that image are returned.}
}
\value{
A list with \code{signal}, a cells by markers numeric matrix, \code{source}, a
character matrix describing the selected source, and \code{policy}, the
validated policy. Source values are \code{"<compartment>:<statistic>"},
\code{"fallback:<compartment>:<statistic>"} or \code{"unavailable"}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Applies a \code{cs_signal_policy} to the intensity measurements in a
\code{cellspec} object. A preferred value is used when it is non-missing and at
least the row's \code{min_value}; otherwise the configured fallback is tested.
}
\examples{
x <- cs_example()
policy <- cs_signal_policy(cs_markers(x))
selected <- cs_signal_matrix(x, policy)
selected$signal[, 1:2]
}
\seealso{
\code{\link[=cs_signal_policy]{cs_signal_policy()}}

Other signals:
\code{\link[=cs_marker_map]{cs_marker_map()}},
\code{\link[=cs_read_signal_policy]{cs_read_signal_policy()}},
\code{\link[=cs_signal_policy]{cs_signal_policy()}},
\code{\link[=cs_write_signal_policy]{cs_write_signal_policy()}}
}
\concept{signals}
