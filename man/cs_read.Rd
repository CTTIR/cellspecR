% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-table.R
\name{cs_read}
\alias{cs_read}
\title{Read a delimited table into a cellspec object}
\usage{
cs_read(
  path,
  format = "auto",
  pixel_size = NULL,
  image_id = NULL,
  sample_id = NULL,
  marker_map = NULL,
  column_map = NULL,
  tile_bounds = NULL,
  verify_done = TRUE,
  keep_other = TRUE,
  keep_paths = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{path}{One delimited table file.}

\item{format}{Reader format. Use \code{"table"} for this adapter or \code{"auto"}
to request conservative detection.}

\item{pixel_size}{Micrometres per pixel. Required for pixel coordinates;
micrometre-coordinate tables use \code{1} when no calibration is supplied.}

\item{image_id}{Optional scalar image identifier overriding the mapped
column; otherwise the mapped column or file basename is used.}

\item{sample_id}{Optional scalar sample identifier overriding the mapped
column; otherwise the mapped column or image identifier is used.}

\item{marker_map}{Reserved for future adapters; not used by the table
adapter.}

\item{column_map}{A validated object returned by \code{\link[=cs_column_map]{cs_column_map()}}.}

\item{tile_bounds}{Optional tile-bound data frame for \code{format = "qupath_tiled"}, with \code{tile_id}, \code{xmin}, \code{xmax}, \code{ymin} and \code{ymax} in
micrometres.}

\item{verify_done}{Whether tiled readers should verify optional \code{.done.tsv}
metadata against each source file.}

\item{keep_other}{Whether unmapped numeric columns become \code{other}
measurements and other unmapped columns are retained in \code{cells}.}

\item{keep_paths}{Whether the full input path may be recorded in
provenance.}

\item{quiet}{Reserved for consistency with future readers; suppresses the
informational warning about dropped columns when \code{keep_other = FALSE}.}
}
\value{
A validated \code{cellspec} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Reads a delimited cell table using a format-specific adapter or an explicit
\code{\link[=cs_column_map]{cs_column_map()}}. See \code{\link[=cs_formats]{cs_formats()}} for supported formats.

The \code{segmantr} adapter expects the one-based row and column centroid
means returned by \code{segmantR::sg_extract_features()}.
It subtracts 0.5 before converting to micrometres and scales pixel \code{area}
by \code{pixel_size^2}. Coordinates refer to the supplied image: cropped or
downsampled inputs require an explicit external frame transform before
combining with a full-resolution image. This adapter assumes square pixels.
Shape measurements retain source units in the feature dictionary.
}
\examples{
path <- tempfile(fileext = ".csv")
utils::write.csv(data.frame(id = c("c1", "c2"), x = 1:2, y = 3:4),
                 path, row.names = FALSE)
map <- cs_column_map("id", "x", "y")
x <- cs_read(path, format = "table", column_map = map)
dim(x)
}
\seealso{
\code{\link[=cs_formats]{cs_formats()}}, \code{\link[=cs_detect_format]{cs_detect_format()}}, \code{\link[=cs_column_map]{cs_column_map()}}

Other adapters:
\code{\link[=cs_column_map]{cs_column_map()}},
\code{\link[=cs_detect_format]{cs_detect_format()}},
\code{\link[=cs_formats]{cs_formats()}}
}
\concept{adapters}
