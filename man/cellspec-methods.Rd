% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{cellspec-methods}
\alias{cellspec-methods}
\alias{print.cellspec}
\alias{summary.cellspec}
\alias{print.cellspec_summary}
\alias{dim.cellspec}
\title{Print, summarise and measure a cellspec object}
\usage{
\method{print}{cellspec}(x, ...)

\method{summary}{cellspec}(object, ...)

\method{print}{cellspec_summary}(x, ...)

\method{dim}{cellspec}(x)
}
\arguments{
\item{x, object}{A \code{cellspec} object (or, for the summary print method, a
\code{cellspec_summary}).}

\item{...}{Unused; for compatibility with the generics.}
}
\value{
\itemize{
\item \code{print()} returns \code{x} invisibly.
\item \code{summary()} returns a data frame of class \code{cellspec_summary} with one row
per image and the columns \code{image_id}, \code{sample_id} (character), \code{n_cells},
\code{n_markers}, \code{n_features} (integer; markers and features with at least
one non-missing value in that image), \code{pixel_size}, \code{x_min}, \code{x_max},
\code{y_min}, \code{y_max} (double, micrometres) and \code{na_fraction} (double,
fraction of missing measurement values in that image).
\item \code{dim()} returns an integer vector \code{c(cells, features)}.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{print()} shows a compact overview that fits in 80 columns: cells, images,
samples, features by kind, markers, pixel size, specification version and
the adapter that produced the object. \code{summary()} returns one row per
image. \code{dim()} returns the number of cells and features.
}
\examples{
x <- cs_example()
x
summary(x)
dim(x)
}
\seealso{
Other methods:
\code{\link[=[.cellspec]{[.cellspec()}},
\code{\link[=as.data.frame.cellspec]{as.data.frame.cellspec()}},
\code{\link[=plot.cellspec]{plot.cellspec()}}
}
\concept{methods}
