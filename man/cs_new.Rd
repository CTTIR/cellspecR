% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/object.R
\name{cs_new}
\alias{cs_new}
\title{Create a cellspec object}
\usage{
cs_new(
  cells,
  measurements = NULL,
  dictionary = NULL,
  images,
  channels = NULL,
  provenance = NULL,
  adjacency = NULL
)
}
\arguments{
\item{cells}{A data frame with one row per cell and at least the columns
\code{cell_id}, \code{image_id}, \code{sample_id} (or a \code{sample_id} in \code{images}), \code{x} and
\code{y} (centroids in micrometres).}

\item{measurements}{A numeric matrix (or data frame of numeric columns) with
one row per cell, in the row order of \code{cells}, and column names equal to
\code{dictionary$feature_id}. \code{NULL} means no features.}

\item{dictionary}{A data frame describing each measurement column (see
\code{\link[=cs_vocabulary]{cs_vocabulary()}}). \code{NULL} is allowed only when there are no features.}

\item{images}{A data frame with one row per image and at least \code{image_id},
\code{sample_id} and \code{pixel_size}.}

\item{channels}{A data frame with one row per image and channel
(\code{image_id}, \code{channel_index}, \code{channel_name}, \code{marker}), or \code{NULL} when no
intensity features exist.}

\item{provenance}{A list describing the origin of the data. Missing fields
are filled with defaults; \code{NULL} records the object as built by
\code{cs_new()}.}

\item{adjacency}{Optional data frame of cell-cell contacts
(\code{image_id}, \code{cell_id_a}, \code{cell_id_b}, \code{shared_boundary}, \code{method}).}
}
\value{
A \code{cellspec} object: a list of class \code{"cellspec"} with elements
\code{spec_version} (string), \code{cells} (data frame), \code{measurements} (double
matrix), \code{dictionary}, \code{images}, \code{channels} (data frames), \code{provenance}
(list) and \code{adjacency} (data frame or \code{NULL}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Builds a \code{cellspec} object from its components and checks it against the
structure rules of the specification. Tool exports are normally read with
\code{cs_read()}; use \code{cs_new()} when you already hold the pieces in memory, for
example in another package that produces cell tables.

Inputs are normalised before validation: any data frame subclass becomes a
plain \code{data.frame} without row names, factors become character vectors,
identifier columns (\code{cell_id}, \code{image_id}, \code{sample_id}, \code{subject_id}) are
converted to character, whole-number doubles in integer columns become
integers, and \code{NaN}/\code{Inf} in \code{measurements} become \code{NA} (counted in
\code{provenance$counts$nonfinite_converted}). A missing \code{sample_id} column in
\code{cells} is filled from \code{images}.
}
\examples{
cells <- data.frame(
  cell_id = c("1", "2", "3"),
  image_id = "img1",
  sample_id = "s1",
  x = c(10, 20, 30),
  y = c(5, 15, 25),
  area = c(50, 62.5, 48)
)
measurements <- cbind(
  "cell:CD3e:mean" = c(120, 8, 95),
  "nucleus:FOXP3:mean" = c(3, 1, 40)
)
dictionary <- data.frame(
  feature_id = colnames(measurements),
  kind = "intensity",
  marker = c("CD3e", "FOXP3"),
  compartment = c("cell", "nucleus"),
  statistic = "mean",
  unit = "a.u.",
  source_name = c("Cell: CD3e: Mean", "Nucleus: FOXP3: Mean")
)
images <- data.frame(image_id = "img1", sample_id = "s1", pixel_size = 0.5)
channels <- data.frame(
  image_id = "img1",
  channel_index = 1:3,
  channel_name = c("DAPI", "CD3e", "FOXP3"),
  marker = c("DAPI", "CD3e", "FOXP3")
)
x <- cs_new(cells, measurements, dictionary, images, channels)
x
}
\seealso{
\code{vignette("specification", package = "cellspecR")} for every
column and rule; \code{\link[=cs_validate]{cs_validate()}} for the checks applied.

Other object:
\code{\link{cs_accessors}},
\code{\link[=cs_bind]{cs_bind()}},
\code{\link[=cs_example]{cs_example()}},
\code{\link[=cs_features]{cs_features()}},
\code{\link[=cs_merge_tiles]{cs_merge_tiles()}},
\code{\link[=is_cellspec]{is_cellspec()}}
}
\concept{object}
