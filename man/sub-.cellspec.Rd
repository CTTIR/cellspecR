% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{[.cellspec}
\alias{[.cellspec}
\title{Subset a cellspec object by cells and features}
\usage{
\method{[}{cellspec}(x, i, j, ..., drop = FALSE)
}
\arguments{
\item{x}{A \code{cellspec} object.}

\item{i}{Cells to keep: a logical vector with one value per cell, or
positive or negative integer positions. Missing means all cells.}

\item{j}{Features to keep: a character vector of feature identifiers, a
logical vector with one value per feature, or integer positions. Missing
means all features.}

\item{...}{Not used; must be empty.}

\item{drop}{Not used; subsetting always returns a \code{cellspec} object.}
}
\value{
A \code{cellspec} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{x[i, j]} keeps the cells selected by \code{i} and the features selected by \code{j}.
All components stay aligned: \code{cells} and \code{measurements} rows are subset
together, dictionary rows follow the kept features, and adjacency contacts
whose cells were removed are dropped. \code{images} and \code{channels} are kept
unchanged.
}
\examples{
x <- cs_example()
dim(x)
big <- x[cs_cells(x)$area > 60, ]
dim(big)
means <- x[, cs_features(x, statistic = "mean")]
dim(means)
}
\seealso{
Other methods:
\code{\link[=as.data.frame.cellspec]{as.data.frame.cellspec()}},
\code{\link{cellspec-methods}},
\code{\link[=plot.cellspec]{plot.cellspec()}}
}
\concept{methods}
