% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{as.data.frame.cellspec}
\alias{as.data.frame.cellspec}
\alias{as_tibble.cellspec}
\title{Convert a cellspec object to a data frame}
\usage{
\method{as.data.frame}{cellspec}(x, row.names = NULL, optional = FALSE, ..., features = NULL)

\method{as_tibble}{cellspec}(x, ..., features = NULL)
}
\arguments{
\item{x}{A \code{cellspec} object.}

\item{row.names, optional}{Ignored; present for compatibility with the
generic.}

\item{...}{Unused.}

\item{features}{Character vector of feature identifiers to include
(default: all features).}
}
\value{
A \code{data.frame} with one row per cell: the \code{cells} columns followed
by one double column per selected feature. \code{as_tibble()} returns the
same content as a tibble (requires the tibble package).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Combines the \code{cells} columns with selected measurement columns into one
wide data frame, for example to hand a table to modelling code.
Measurement columns are named by their feature identifiers.
}
\examples{
x <- cs_example()
df <- as.data.frame(x, features = cs_features(x, statistic = "mean"))
head(df)
}
\seealso{
Other methods:
\code{\link[=[.cellspec]{[.cellspec()}},
\code{\link{cellspec-methods}},
\code{\link[=plot.cellspec]{plot.cellspec()}}
}
\concept{methods}
