% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal.R
\name{cs_signal_policy}
\alias{cs_signal_policy}
\title{Create a signal selection policy}
\usage{
cs_signal_policy(
  marker,
  compartment = "cell",
  statistic = "mean",
  fallback_compartment = NA_character_,
  min_value = 0
)
}
\arguments{
\item{marker}{Character vector of unique marker names.}

\item{compartment}{Preferred compartment for each marker. A single value is
recycled across \code{marker}.}

\item{statistic}{Intensity statistic for each marker. A single value is
recycled across \code{marker}.}

\item{fallback_compartment}{Optional fallback compartment for each marker;
\code{NA} disables fallback. A single value is recycled across \code{marker}.}

\item{min_value}{Minimum usable value for each marker. Values below this
threshold are unavailable. A single value is recycled across \code{marker}.}
}
\value{
A data frame with class \code{cs_signal_policy} and columns \code{marker},
\code{compartment}, \code{statistic}, \code{fallback_compartment} and \code{min_value}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Creates one validated row per marker. Signal policies make the selected
compartment, statistic, fallback and minimum usable value explicit.
}
\examples{
cs_signal_policy(
  marker = c("PanCK", "FOXP3"),
  compartment = c("cytoplasm", "nucleus"),
  fallback_compartment = "cell"
)
}
\seealso{
\code{\link[=cs_signal_matrix]{cs_signal_matrix()}}, \code{\link[=cs_marker_map]{cs_marker_map()}}

Other signals:
\code{\link[=cs_marker_map]{cs_marker_map()}},
\code{\link[=cs_read_signal_policy]{cs_read_signal_policy()}},
\code{\link[=cs_signal_matrix]{cs_signal_matrix()}},
\code{\link[=cs_write_signal_policy]{cs_write_signal_policy()}}
}
\concept{signals}
