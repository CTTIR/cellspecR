% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{cs_vocabulary}
\alias{cs_vocabulary}
\title{Columns and controlled vocabularies of the cellspec format}
\usage{
cs_vocabulary(what = c("columns", "vocabularies", "checks"))
}
\arguments{
\item{what}{Which table to return: \code{"columns"}, \code{"vocabularies"} or
\code{"checks"}.}
}
\value{
For \code{what = "columns"}, a data frame with one row per specified
column and the character columns \code{component} (\code{"cells"}, \code{"dictionary"},
\code{"images"}, \code{"channels"}, \code{"adjacency"}), \code{column}, \code{type} (R storage
type), \code{requirement} (\code{"MUST"}, \code{"SHOULD"}, \code{"MAY"}) and \code{description}.
For \code{what = "vocabularies"}, a data frame with the character columns
\code{vocabulary}, \code{value} and \code{description}. For \code{what = "checks"}, the
validation catalogue used by \code{\link[=cs_validate]{cs_validate()}}: character columns \code{check},
\code{level} (\code{"structure"} or \code{"semantic"}) and \code{description}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Returns the specification tables as data: the columns each component may
carry, and the allowed values of every controlled vocabulary. The
validator (\code{\link[=cs_validate]{cs_validate()}}) enforces exactly these tables, so other
packages can use them to build compatible objects.
}
\examples{
cols <- cs_vocabulary("columns")
cols[cols$component == "cells", c("column", "type", "requirement")]

voc <- cs_vocabulary("vocabularies")
voc$value[voc$vocabulary == "compartment"]

head(cs_vocabulary("checks"))
}
\seealso{
\code{vignette("specification", package = "cellspecR")}

Other specification:
\code{\link[=cs_spec_version]{cs_spec_version()}}
}
\concept{specification}
